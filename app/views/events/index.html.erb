<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
    .table {
      width: 900px;
      margin-left: auto;
      margin-right: auto;
    }
    </style>
  </head>
  <body>

    <table class="table">
      <thead>
        <tr>
          <th scope="col" colspan="3">
            <% if @category == nil %>
            Все события
            <% else %>
            <%= @category.title %>
            <% end %>
          </th>
          <th scope="col">1</th>
          <th scope="col">X</th>
          <th scope="col">2</th>
          <th scope="col"></th>
        </tr>
      </thead>
      <tbody>
        <% @events.each do |event| %>
        <tr>
          <td>
          <% if event.result.present?%>
          Завершен
          <% else %>
          <%= event.dattime.strftime("%d %b %Y %H:%M")%>
          <% end %>
          </td>
          <td><%= event.team1%></td>
          <td><%= event.team2%></td>
          <td><%= event.win_ratio_1%></td>
          <td><%= event.draw_ratio%></td>
          <td><%= event.win_ratio_2%></td>
          <td>
            <% if current_user.present? %>
              <% if event.result.present? %>

              <% else %>
                <% if current_user.role == "user" %>
                  <% if @cart.include?(event) %>
                  <form action="/event/remove_from_cart/<%= event.id %>" method="POST">
                    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                    <button type="submit" class="btn btn-primary" style="width: 100px;">Удалить из купона</button>
                  </form>
                  <% else %>
                  <form action="/event/add_to_cart/<%= event.id %>" method="POST">
                    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                    <button type="submit" class="btn btn-primary" style="width: 100px;">Добавить в купон</button>
                  </form>
                  <% end %>
                <% elsif current_user.role == "admin"%>
                  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
                  <button type="button" class="btn btn-primary" style="width: 100px;">Обновить</button>
                <% end %>
              <% end %>
            <% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </body>
</html>
